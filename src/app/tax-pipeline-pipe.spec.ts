import { TaxPipelinePipe } from './tax-pipeline-pipe';

describe('TaxPipelinePipe', () => {
  it('create an instance', () => {
    const pipe = new TaxPipelinePipe();
    expect(pipe).toBeTruthy();
  });
});
